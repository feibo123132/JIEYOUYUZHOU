# 2026-08-29 工作日志

## 谱子功能（方案 A）完成 ✅

用户核心痛点：歌曲多、谱子都在 iPad 上，演唱时找谱麻烦 → 为每首歌配专属谱子，电脑上传、iPad 云端查看。选定方案 A：歌曲详情面板谱子区块 + 全屏翻谱器 + 云同步。

### 新增文件
- `src/components/SongRequest/songScores.ts`：数据层。类型 `SongScore`、localStorage 缓存（前缀 `jieyou-song-scores-v1:`）、页码记忆、canvas 图片压缩（长边 1500px / JPEG 0.72 / 白底填 PNG 透明）
- `src/components/SongRequest/ScoreViewer.tsx`：全屏翻谱器。键盘 ←/→/Esc、触摸滑动翻页（水平位移>48px 且 >1.5×垂直位移）、双击缩放、页码 localStorage 记忆

### 修改文件
- `SongDetailPanel.tsx`：标题卡片与记录 grid 之间插入「专属谱子」区块（上传/删除/排序/清空/打开翻谱器，缩略图 aspect-[3/4]）
- `SongRequestStation.tsx`：`songScores` state + pull 同步 effect + `commitSongScore`（本地缓存优先 + 云端 save/delete，失败 toast）
- `songRequestCloud.ts`：`pullSongScores` / `saveSongScore` / `deleteSongScore` / `mapSongScoreSyncError`
- `cloudfunctions/songRequestSync/{index,validation}.js`：新 action `songScores:pull/save/delete`，新集合 `song_request_song_scores`（文档 id = sha256(workspace:score:songId)，软删除），3MB 请求上限 / 2MB 文档上限 / 每首 ≤16 页

### 关键参数
- 每页 https URL ≤600 字符，或 base64 dataURL + magic bytes 校验
- 压缩后单页约 150-250KB

### 验证结果
- `tsc --noEmit` 通过
- 云函数测试 `songRequestSyncFunction.test.cjs` 21/21 过
- 前端 92 测试 82 过 10 挂 —— **10 个失败全部为既有失败**（识曲歌库→听歌识曲、个人练习榜→吉他练习榜改名遗留 + RecordTimeline 形态断言 + playlists 布局演进），与谱子改动零交集
- 89 号详情页测试：`role="status"`→`aria-live="polite"` 修复已生效（1608 doesNotMatch 通过），仅剩 RecordTimeline 形态既有断言失败

### 待办：云函数部署
- 部署包已生成：`artifacts/songRequestSync-scores-20260829.zip`（index.js + validation.js + package.json 平铺，与历史部署包同构）
- 需在腾讯云控制台上传部署后，谱子云同步才生效

### 教训
- **git stash 在无 commit 的仓库上会 segfault**（exit 139，branch 无 HEAD），且会把所有 untracked 文件加入 index。此仓库从未 commit，勿用 stash/diff 对比基线，改用文件内容核对
