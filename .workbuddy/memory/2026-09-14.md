

## 会话记录「暂无对话记录」排查（19:51）
- 现象：聊天列表正常，但打开旧会话显示「暂无对话记录」；14:04 用户已发现并开窗排查过（该会话 da6bc3ee 即 0914 新窗口③）。
- 结论：数据未丢失。所有会话转录完整存于 ~/.workbuddy/projects/<项目slug>/<sessionId>.jsonl（JIEYOU 项目 13 个文件共 30MB）；sessions 表 75 行完好；19:51 日志显示 resume initialize() 成功但 UI 未回放历史 → 客户端显示层 bug。
- 处置：写 export_chat_20260914.py（.workbuddy/ 下），把 5 个会话导出为 artifacts/聊天记录导出_20260914.html（575KB，衬线排版、工具调用折叠）。
- 另：下午「无法安装必要的更新」弹窗排查过，Trae/WorkBuddy 安装目录（AppData/Local/Programs）下无用户项目，判定误报。
- 坑：本会话 bash 环境损坏（ls/dirname/head 全 missing），文件操作用 python -c + 正斜杠路径；大段脚本写 .py 文件执行。
