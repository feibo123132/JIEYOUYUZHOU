# 2026-09-12

## 「星语心愿」时间显示按身份分级

- 需求：站主本人看完整日期时间（2026年9月11日 21:07），非站主只看年份+月份（2026年9月）。
- `src/components/StarrySky/StarMessagesPage.tsx`：
  - 新增 `isStationOwner` state，mount 时通过 `readSongRecordSession(sessionStorage)` + `STAR_OWNER_ALIAS` 判断。
  - 派生 `effectiveShowTimeDetails = isStationOwner && showTimeDetails`，传给 `formatStarMessageTime`。
  - 助手栏里"时间详情/隐藏/显示"按钮仅在 `isStationOwner` 时渲染；非站主描述文案改为「仅站主本人可见完整日期时间」。
- 类型检查通过。